apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: dataprovider
  name: files-rsrv-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: filesrsrv.192.168.49.2.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: files-rsrv-svc
                port:
                  number: 9095
