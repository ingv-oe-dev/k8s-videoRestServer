apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: videorestserver-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/ingv-oe-dev/k8s-videoRestServer.git
    targetRevision: main
    path: videorestserver/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: dataprovider

  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true